{
    "navigationFallback": {
        "rewrite": "/index.html"
    },
    "globalHeaders": {
      "x-frame-options": "SAMEORIGIN",
      "Permissions-Policy": "fullscreen=(self), geolocation=(), microphone=(), camera=(), fullscreen=(self), payment=()"
    }
}
